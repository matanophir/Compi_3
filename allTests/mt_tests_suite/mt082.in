// Test mt082.in – binop
// Expected: PASS
//binop
//comp_mix_arith_logic_mismatch
void main(){
    int  x = 1;
    int  y = 2;
    if((x + y) && true){   // (x+y) is int → mismatch in &&
    }
    return;
}
